<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= post.title %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <nav>
    <a href="/posts">Home</a> |
    <a href="/posts/create">Create Post</a> |
    <a href="/about">About</a>
  </nav>

  <h1><%= post.title %></h1>
  <p><small>By <%= post.username %> | <%= post.createdAt.toDateString() %></small></p>
  <p><%= post.content %></p>

  <a href="/posts/<%= post.id %>/edit">Edit</a> |
  <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" style="display:inline;">
    <button onclick="return confirm('Are you sure you want to delete this post?')">Delete</button>
  </form>
</body>
</html>
